---
title: "Coffe-project"
---

```{r}
# Coffee Quality Analysis

## 1 - Introducción

# Bloque de códigos
library(readr)
library(dplyr)
library(ggplot2)
library(plotly)
library(DT)

```

```{r}
coffee_data <- read.csv("C:/Users/PC/Desktop/Proyecto2/coffee-quality.csv")
```

```{r}
# Bloque de código para generar la tabla interactiva con el paquete DT
datatable(coffee_data, 
          options = list(pageLength = 10),
          filter = "top",
          rownames = FALSE,
          colnames = c("Country_of_Origin", "Variety", "Color", "Altitude", "Total_Cup_Points"))

```

```{}
```

```         
```

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

```{r}
1 + 1
```
